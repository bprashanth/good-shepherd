# Practitioner Stack Home

This stage mirrors `examples/stacks/research/web/` and is implemented as a direct card-based entrypoint.

It is a static entrypoint that links to the practitioner stack components:

- where to intervene
- what is growing there
- what to plant there
- monitoring
- data
- recovery

Two of those cards point to the same underlying PWA implementation in `examples/stacks/rewild/pwa/`, and the `data` card reuses the shared `examples/stacks/research/forms` dev server on `localhost:5173`.
